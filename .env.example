# No environment variables are required to run ChangeLens locally.
# Demo data, evidence files, and temporary AI test keys are stored in this
# browser's local IndexedDB via Dexie. No backend is required.
